version: '3'
services:
  frontend:
    build:
      context: ./Frontend
    ports:
      - "3000:3000"
  backend:
    build:
      context: ./Backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=mongodb+srv://federicorouyere:mongo1234@prueba-cluster.cso4kl6.mongodb.net/